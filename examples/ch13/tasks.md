# 任務範本（貼入對話，不是 Shell）

> 請先讀 fee.py 與 test_fee.py，實際執行 python3 -m unittest -v，確認失敗是三張一般票缺少九折。只修改 fee.py，保留票種與正整數驗證，不修改測試的預期值。用最小變更實作三張以上九折，再執行測試。回報修改位置、測試命令與退出狀態；如果無法執行，明確標示待驗證。不要安裝套件，不要提交 Git。

## 人工批准與必做回歸

先用 management-lab.md 核定 fee-v1。上方只改 fee.py；此步通過後另授權新增 test_fee.py 方法，保留原案例及預期值，必補學生三／四張、門檻前一張與非法輸入（含 True）。獨立 acceptance.py 不改。完整補測參考在 reference/test_fee.py。模型交付與作者參考分欄記錄，實跑兩命令後由人決定是否准私人整合。
