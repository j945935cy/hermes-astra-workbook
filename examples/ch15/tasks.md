# 任務範本（貼入對話，不是 Shell）

> 請使用兩個子代理平行檢查活動資料。工作目錄是：填入剛才 pwd 的完整輸出。先讀 contract.md，確認 sessions.csv 存在。A 只讀這兩個檔案，只寫 capacity.md，依 registered > capacity 列出超收 ID、差額與來源數值。B 只讀同樣輸入，只寫 ownership.md，列出 confirmed 且缺 owner 的 ID，說明 draft 的排除規則。把完整工作目錄、規則、檔名與禁止修改來源的限制分別傳給兩位子代理。子代理各自驗證報告後回報路徑與限制。請等兩份結果到齊，再由主代理讀回來源及報告，寫 final.md；不要讓子代理互改報告，不要提交 Git。

## 先選路線與停止上限

人工主線：按 management-lab.md 的 A、B 工作單分別保存 capacity.md、ownership.md，再由本人讀回來源與兩報告生成 final.md，稱分工演練完成，不需 delegation。上方才是真實委派選做；須先記輸入／契約版、截止、呼叫／重試／人工審查上限與整合決策者。記兩次真實 ID、完成事件與讀回才可稱 delegation 通過（仍待實測）。unknown／失敗不是零問題；遲到先核版，已批准 final 不覆寫；接手先查舊狀態並用不同新輪目錄。
