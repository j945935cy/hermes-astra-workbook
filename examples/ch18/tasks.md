# 任務範本（貼入對話，不是 Shell）

> 在任何輸出寫入前先檢查三個目標；任一已存在（含符號連結）就停止，不覆寫、不刪除、不自行改名。只有這次從空目標開始所產生的檔案，才可在同次執行中修正；重啟遇到部分輸出也停止。再讀 task-spec.md、verify.py 與 input 內兩份素材，說明不確定事項是否已有處理規則。只在 output 目錄建立 actions.csv、report.md、README.md，不修改輸入、規格或驗收器。完成後實際執行 python3 verify.py。若驗收失敗，根據批准契約修正輸出，不更改預期答案。報告必須分清楚確定工作與待確認建議，不自行補負責人或日期。最後回報三個檔案路徑、實跑結果、仍需人工審核的地方。只交本機草稿，不寄信、不公開、不設定排程。

## 人工結案與第二期

第一期核心已有主編限定通過摘要 verification.md；第二期模型正常流程另有編輯端限定證據，但真人簽核／接手及第二期模型停止變體未測。不使用 reference/output 冒稱模型產物。人另存 graduation-closeout.md 和 failure-handling.md（management-lab.md），三檔之外保存來源版本、機械驗收、語義審閱、讀者本次停止行為證據（未做就填待測）、准用範圍與責任人。預設只私人本機，不寄送排程。第二期使用 period2/README.md 的獨立目錄，先核定 approved-baseline.md 與當期 verify.py 才開模型，不更動第一期。
