# 第18章驗證範圍

## 第一期：主編真實模型限定通過

素材版本：`a621aa4233952947f5fbd997937ed206fbd9b95a`。既有 WSL 安裝，以 Hermes CLI、`--safe-mode`、明確 `--in` 工作目錄執行；本機 session 紀錄確認模型 `gpt-6-astra`、供應商 `openai-codex`，不是服務端獨立身分認證。

| 情境 | 主編讀回結果 |
|---|---|
| normal | 三檔 actions.csv／report.md／README.md 正確；程序、獨立 verify 退出 0；來源／契約／驗收器 hash 不變 |
| existing-output | 真實模型明確停止，無新增檔，全部 fixture hash 不變 |
| missing-source | 真實模型明確停止，無新增檔，全部 fixture hash 不變 |

獨立內容審閱：僅 G02/G03/G04 且順序正確；G03 owner、G04 due 保持待確認；未把建議日期寫成批准；N01/N02/N03 對應正確；README 說明停止、重跑、新期來源快照與本機草稿限制。

先前 v1 因繼承的工作目錄未被 subprocess cwd 覆蓋而失敗、沒有產物；改明確 `--in` 後 v2 通過。前置 Python 單行檢查曾被安全層阻擋，改唯讀 Shell 檢查，沒有降低安全設定。提示限制不是沙箱。原始私人 log 與個人路徑不公開；本頁只是主編已查核的摘要，不是可移植的安裝保證。

## 第二期：確定性驗收與編輯端模型試跑

`period2/test_period2.py` 實跑 12 tests OK（Python 3.14.4、既有 WSL）。包含完整作者參考、空輸出、錯 G04 日期、漏 G05、錯映射、未知狀態、重複、改來源、缺文件及準備腳本保護。從 repo 根目錄執行：

```bash
python3 -B -m unittest discover -s examples/ch18/period2 -p 'test_period2.py' -v
```

第二期準備腳本在新含空白目錄 READY；空成果驗收退出 1（缺 actions.csv）；僅在暫存測試副本放入作者參考後驗收退出 0。其後另以真實 Astra CLI 正常生成三檔，獨立驗收與內容讀回通過；G04 按 N05 採用批准日期並保留 N03 歷史，G05 完整納入，G03 負責人仍待確認。五個來源／規格檔與第一期證據皆未改；錯日期變異由當期驗收器拒絕。這是作者虛構基線下的編輯端模型測試，未捏造真人批准紀錄，不等於真人簽核或接手試讀。首次試跑遭供應商串流逾時而無產物；另起新副本，以單次 `--reasoning low` 重跑通過，未改永久設定。準備腳本另修正缺必要檔仍 READY 的缺陷，新增缺素材／批准基線及來源符號連結拒絕回歸；檔案檢查在建立目的地之前完成。

## 尚未涵蓋

新機 Windows／WSL2 安裝、其他平台、目錄或符號連結目標的模型停止變體、初學者試讀、真人簽核／接手、第二期模型停止變體、排程、通知、MCP 正式服務／完整生命週期。實際金額及人工基準未知，不推估生產率。讀者仍須記錄自己的驗收，不抄作者簽收。
